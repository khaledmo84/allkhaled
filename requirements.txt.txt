# =============================================================================
# requirements.txt – AlKhaled Ultimate (Production Only)
# الإصدار: 2.0.0 – خالٍ من الثغرات المعروفة
# =============================================================================
# هذا الملف يحتوي على المكتبات الضرورية فقط لتشغيل البوت.
# تمت ترقية جميع المكتبات إلى إصدارات آمنة.
# =============================================================================

# -----------------------------------------------------------------------------
# الأساسيات (Core)
# -----------------------------------------------------------------------------
aiohttp>=3.9.5
aiosqlite>=0.20.0
redis>=5.0.3
python-dotenv>=1.0.1
python-multipart>=0.0.9
starlette>=0.38.0
uvloop>=0.19.0

# -----------------------------------------------------------------------------
# البلوكشين والويب 3 (Blockchain & Web3)
# -----------------------------------------------------------------------------
web3>=6.15.0
eth-account>=0.12.0
eth-keys>=0.5.1
eth-utils>=2.3.1

# -----------------------------------------------------------------------------
# الأمان والتشفير (Security & Crypto)
# -----------------------------------------------------------------------------
cryptography>=42.0.5
pysha3>=1.0.2

# -----------------------------------------------------------------------------
# تحليل البيانات والتعلم الآلي (Data & ML)
# -----------------------------------------------------------------------------
numpy>=1.26.4
# -----------------------------------------------------------------------------
# قواعد البيانات والتخزين (Databases & Storage)
# -----------------------------------------------------------------------------
aiomysql>=0.2.0
pymysql>=1.1.1
asyncpg>=0.29.0
aiosqlite>=0.20.0

# -----------------------------------------------------------------------------
# أدوات مساعدة (Utilities)
# -----------------------------------------------------------------------------
requests>=2.31.0
click>=8.1.7
psutil>=5.9.8
aiofiles>=23.2.1

# -----------------------------------------------------------------------------
# مراقبة وتتبع (Monitoring)
# -----------------------------------------------------------------------------
opentelemetry-sdk>=1.24.0
prometheus-client>=0.20.0

# -----------------------------------------------------------------------------
# خوادم وواجهات (Servers & APIs)
# -----------------------------------------------------------------------------
uvicorn>=0.29.0
