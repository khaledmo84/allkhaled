# =============================================================================
# fly.toml – إعدادات النشر على منصة Fly.io
# =============================================================================
# الإصدار: 5.0.0
# التاريخ: 2025-04-19
# =============================================================================
# طريقة الاستخدام:
#   flyctl launch
#   flyctl deploy
# =============================================================================

# اسم التطبيق
app = "alkhaled"

# المنطقة الأساسية (primary region)
primary_region = "iad"

# -----------------------------------------------------------------------------
# إعدادات البناء (Build)
# -----------------------------------------------------------------------------
[build]
  image = "ghcr.io/alkhaled/alkhaled:latest"
  dockerfile = "Dockerfile"
  builder = "docker"

[build.args]
  BUILDKIT_PROGRESS = "plain"
  PIP_NO_CACHE_DIR = "1"
  PIP_DISABLE_PIP_VERSION_CHECK = "1"

# -----------------------------------------------------------------------------
# إعدادات الخدمة (Http Service)
# -----------------------------------------------------------------------------
[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = false
  auto_start_machines = true
  min_machines_running = 1
  processes = ["app"]

  [http_service.concurrency]
    type = "connections"
    soft_limit = 200
    hard_limit = 300

# -----------------------------------------------------------------------------
# إعدادات الـ Machine (VM)
# -----------------------------------------------------------------------------
[[vm]]
  memory = "1gb"
  cpu_kind = "shared"
  cpus = 1

# -----------------------------------------------------------------------------
# إعدادات الملفات المؤقتة (tmpfs)
# -----------------------------------------------------------------------------
[[mounts]]
  source = "tmpfs"
  destination = "/tmp"
  size = "512mb"

# -----------------------------------------------------------------------------
# إعدادات التوسع التلقائي (Autoscaling)
# -----------------------------------------------------------------------------
[autoscaling]
  enabled = true
  min_machines = 1
  max_machines = 3
  target_load = 80

# -----------------------------------------------------------------------------
# إعدادات المهام الخلفية (Background processes)
# -----------------------------------------------------------------------------
[processes]
  web = "python run.py --mode web"
  worker = "celery -A tasks worker --loglevel=info"
  beat = "celery -A tasks beat --loglevel=info"

# -----------------------------------------------------------------------------
# إعدادات إعادة المحاولة (Restart)
# -----------------------------------------------------------------------------
[restart]
  policy = "on-failure"
  max_retries = 3

# -----------------------------------------------------------------------------
# إعدادات الصحة (Health Checks)
# -----------------------------------------------------------------------------
[[checks]]
  grace_period = "30s"
  interval = "15s"
  timeout = "10s"
  method = "get"
  path = "/health"
  protocol = "http"
  port = 8081

# -----------------------------------------------------------------------------
# إعدادات السجلات (Logging)
# -----------------------------------------------------------------------------
[logging]
  driver = "json-file"
  [logging.options]
    max-size = "10m"
    max-file = "3"

# -----------------------------------------------------------------------------
# إعدادات الخدمات الإضافية (Add-ons)
# -----------------------------------------------------------------------------
[services]
  [services.redis]
    image = "redis:7-alpine"
    ports = ["6379:6379"]
  
  [services.postgres]
    image = "postgres:15-alpine"
    ports = ["5432:5432"]
    environment = ["POSTGRES_PASSWORD=alkhaled_secure"]

# -----------------------------------------------------------------------------
# إعدادات البيئة (Environment Variables)
# -----------------------------------------------------------------------------
[env]
  ENVIRONMENT = "production"
  LOG_LEVEL = "INFO"
  PORT = "8080"
  HEALTH_PORT = "8081"

# -----------------------------------------------------------------------------
# إعدادات السحابة (Cloud)
# -----------------------------------------------------------------------------
[cloud]
  platform = "fly"
  region = "iad"

# -----------------------------------------------------------------------------
# إعدادات الـ Gateway
# -----------------------------------------------------------------------------
[gateway]
  enabled = true
  proxy_proto = true